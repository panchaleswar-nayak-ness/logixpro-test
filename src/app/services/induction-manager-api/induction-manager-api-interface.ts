export interface IInductionManagerApiService
{
  getPickBatchTransactionTable(body);
   AllBatchDelete();    
   GetFromToteTypeAhead(); 
   SuperBatchIndex();
   NextBatchID();
   ProcessPutAwayIndex(); 
   NextTote(); 
   completeTransaction(body);
   completePickBatch(body);
   shortTransaction(body);
   blossomTote(body); 
   ClearPickToteInfo(body);
   SelectBatchPickTA(body);
   SelectToteTransManTable(body);
   CompName();  
   PreferenceIndex();
   PickBatchFilterRename(body);   
   ProcessBlossom(body);   
   PickBatchZonesSelect(body);
   PickBatchFilterTypeAhead(body);
   PickBatchDefaultFilterMark(body);
   PickBatchDefaultFilterClear(body);
   PickBatchDefaultFilterSelect(body);
   PickBatchFilterBatchDelete(body);
   OrdersFilterZoneSelect(body);
   PickToteTransDT(body);
   PickBatchFilterOrderData(body);
   PickBatchFilterUpdate(body);
   PickBatchFilterInsert(body);
   PickBatchOrderUpdate(body);
   PickBatchOrderInsert(body);
   PickBatchOrderDelete(body);
   PickBatchZoneDefaultMark(body);
   PickBatchFilterDelete(body);
   OrdersInZone(body);
   WSPickZoneSelect(body);
   PickToteSetupIndex(body);
   FillOrderNumber(body);
   ValidateOrderNumber(body);
   InZoneSetupProcess(body);
   PickToteSetupProcess(body);
   LocationZonesSelect(body);
   WSPickZoneInsert(body);
   WSPickZoneDelete(body);
   ClrWSPickZone(); 
   InZoneTransDT(body);
   TaskComplete(body);  
   CrossDock(body);
   FindLocation(body);  
   CheckForwardLocations(body);  
   IMUpdate(body); 
   ItemDetails(body);  
   AvailableZone(body);  
   RPDetails(body); 
   CompletePick(body);  
   BatchLocationTypeAhead(body) 
   ReserveLocation(body);  
   BatchTotesDelete(body);   
   TransTableView(body);  
   TransactionForTote(body);  
   NextSerialNumber(body);
   ReelsCreate(body);
   ValidateSn(body);  
   BatchByZone(body);    
   ItemZoneDataSelect(body);
   SuperBatchCreate(body);
   TotePrintTableInsert(body);
   ReqDateDataSelect();  
   MarkToteFull(body);
   CompleteBatch(body);
   TotesTable(body);
   BatchIDTypeAhead(body);
   NextToteUpdate(body);
   ProcessBatch(body);
   ValidateTotesForPutAways(body);
   BatchExist(body);
   BatchTotes(body);  
   ValidateTote(body);
   ValidateItem(body);
   ProcessPallet(body);
   ValidateSerialNumber(body);
   DeleteSerialNumber(body);
}