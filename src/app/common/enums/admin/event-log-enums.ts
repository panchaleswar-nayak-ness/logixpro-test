/**
 * Enum defining filter field names for Event Log component
 * Used to map UI field names to component properties in a type-safe way
 */
export enum EventLogFilterField {
  Message = 'message',
  EventLocation = 'eventLocation',
  EventCode = 'eventCode',
  EventType = 'eventType',
  UserName = 'userName'
}

