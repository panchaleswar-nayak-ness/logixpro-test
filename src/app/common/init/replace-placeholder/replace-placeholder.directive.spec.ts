import { ReplacePlaceholderDirective } from './replace-placeholder.directive';

describe('ReplacePlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplacePlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
